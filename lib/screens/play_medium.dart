import 'package:flutter/material.dart';

class PlayMedium extends StatefulWidget {
  @override
  _PlayMediumState createState() => _PlayMediumState();
}

class _PlayMediumState extends State<PlayMedium> {
  // declare as variáveis que você precisará aqui

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        // adicione o botão de voltar para o menu principal aqui
      ),
      /*body: SafeArea(
        // construa a tela aqui
      ),*/
    );
  }
}
